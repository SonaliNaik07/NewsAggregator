import React from 'react';
import '../Styles/Dashboard.css';

const Header: React.FC = () => (
  <header className="header">
    <h1>Dashboard</h1>
    <div className="user-info">Welcome, Sonali 👋</div>
  </header>
);

export default Header;
